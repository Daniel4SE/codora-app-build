# /build - Expo 应用构建部署

一键构建、预览、部署 Expo/React Native 应用。

## 快速使用

```
/build preview          # 🚀 本地预览（最快）
/build android          # 📱 构建 Android APK
/build ios              # 🍎 构建 iOS IPA
/build all              # 📱🍎 构建全部
/build submit           # 🚀 提交到商店
```

## 完整参数

```
/build [action] [platform] [options]
```

### Actions (操作)
| 操作 | 说明 | 耗时 |
|------|------|------|
| `preview` | 本地开发预览 | 几秒 |
| `build` | 云端构建 APK/IPA | 10-15分钟 |
| `submit` | 提交到 App Store/Google Play | 1-5分钟 |
| `update` | OTA 热更新 | 1-2分钟 |

### Platforms (平台)
| 平台 | 说明 |
|------|------|
| `ios` | iOS 模拟器/真机 |
| `android` | Android 模拟器/真机 |
| `all` | 全部平台 |
| `web` | Web 版本 |

### Options (选项)
| 选项 | 说明 |
|------|------|
| `--profile dev` | 开发版本 |
| `--profile preview` | 预览版本 (APK) |
| `--profile production` | 生产版本 (AAB/IPA) |

## 示例

### 本地开发预览（推荐日常使用）
```
/build preview ios          # iOS 模拟器
/build preview android      # Android 模拟器
/build preview              # 启动 Expo，扫码预览
```

### 构建安装包
```
/build android              # 构建 Android APK
/build ios                  # 构建 iOS IPA
/build all                  # 构建全部
```

### 提交商店
```
/build submit ios           # 提交到 TestFlight/App Store
/build submit android       # 提交到 Google Play
/build submit all           # 提交全部
```

### OTA 热更新（不需要重新提交商店）
```
/build update               # 推送代码更新到已安装的 App
```

## 执行流程

```
┌─────────────────────────────────────────────────────────┐
│                    /build 工作流程                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  preview ──→ 启动本地开发服务器 ──→ 模拟器/扫码预览      │
│                                                         │
│  build ──→ 上传到 EAS ──→ 云端构建 ──→ 下载 APK/IPA     │
│                                                         │
│  submit ──→ 获取构建 ──→ 上传商店 ──→ 等待审核          │
│                                                         │
│  update ──→ 打包 JS Bundle ──→ 推送 OTA ──→ 即时生效    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 输出文件

| 类型 | 路径 |
|------|------|
| Android APK | `~/Desktop/{项目名}.apk` |
| iOS IPA | `~/Desktop/{项目名}.ipa` |
| 构建日志 | `https://expo.dev/accounts/{用户}/projects/{项目}/builds` |

## 前置要求

| 要求 | 用途 | 费用 |
|------|------|------|
| Node.js | 运行环境 | 免费 |
| Expo 账户 | 云构建 | 免费 |
| Apple Developer | iOS 发布 | $99/年 |
| Google Play Console | Android 发布 | $25 一次性 |

## 快速设置

首次使用前运行：
```bash
# 安装 EAS CLI
npm install -g eas-cli

# 登录 Expo
eas login

# 初始化项目（在项目目录）
eas build:configure
```

## 常见问题

**Q: 端口被占用怎么办？**
- 自动检测端口冲突，切换到可用端口 (8082, 8083...)
- 也可以选择终止已有进程或连接到现有服务器

**Q: 构建太慢怎么办？**
- 日常开发用 `/build preview`，秒开
- 只在需要分发时用 `/build android/ios`

**Q: 如何更新已发布的 App？**
- 小更新：`/build update`（OTA，即时生效）
- 大更新：修改版本号 → `/build all` → `/build submit`

**Q: iOS 构建需要 Mac 吗？**
- 不需要！EAS 云构建在服务器上完成

**Q: 如何添加测试员？**
- iOS: App Store Connect → TestFlight → 添加测试员
- Android: Google Play Console → 内部测试 → 添加邮箱

## 相关命令

```bash
eas build:list          # 查看构建历史
eas build:view ID       # 查看构建详情
eas update              # OTA 更新
eas submit              # 提交商店
eas credentials         # 管理证书
```

---
**作者**: Daniel Tang
**版本**: 1.1.0
**仓库**: https://github.com/Daniel4SE/codora-app-build
